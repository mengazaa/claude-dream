---
description: "Memory consolidation — reflective pass over your memory files"
args: "[--scan | --quick]"
---

# Dream — Memory Consolidation

Read the full skill documentation first, then execute:

```
~/.claude/skills/dream/SKILL.md
```

**Arguments:**
- `/dream` — Full 4-phase consolidation (default)
- `/dream --scan` — Read-only audit, report problems only
- `/dream --quick` — Light pass, merge duplicates + update index only

Follow the skill instructions exactly. Report progress at each phase milestone.
